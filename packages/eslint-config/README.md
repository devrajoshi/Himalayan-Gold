# 🛠️ @repo/eslint-config

Shared ESLint configurations used throughout the Himalayan Gold monorepo. This package ensures consistent code quality and style across all applications and packages.

## Included Configs

- `next`: Configuration for Next.js applications.
- `library`: Base configuration for shared packages.
